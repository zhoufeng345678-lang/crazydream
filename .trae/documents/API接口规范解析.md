# crazydream API接口规范解析

## 一、认证机制与请求格式

### 1.1 认证方式
- **类型**: JWT Bearer Token
- **格式**: `Authorization: Bearer {token}`
- **获取方式**: 通过登录接口返回

### 1.2 统一请求头
```json
{
  "Content-Type": "application/json",
  "Authorization": "Bearer {token}"
}
```

### 1.3 统一响应格式
```json
{
  "code": 200,
  "message": "成功",
  "data": {}
}
```

## 二、标准错误码

| 错误码 | 描述 | 处理建议 |
|-------|------|----------|
| 200   | 成功 | 正常处理 |
| 400   | 请求参数错误 | 检查参数格式和必填项 |
| 401   | 未授权或认证失败 | 重新登录获取token |
| 403   | 禁止访问，没有权限 | 检查用户权限 |
| 404   | 资源不存在 | 检查资源ID是否正确 |
| 500   | 服务器内部错误 | 联系后端开发人员 |

## 三、核心接口模块详解

### 3.1 认证模块（Authentication）

#### 用户注册
- **接口名称**: 用户注册
- **请求方法**: POST
- **URL路径**: `/auth/register`
- **是否需要认证**: 否
- **前端依赖模块**: 认证模块 (Auth Module)
- **请求参数**:
  ```json
  {
    "email": "string(必填, 邮箱格式)",
    "password": "string(必填, 8-20位)",
    "nickName": "string(必填, 2-20字符)",
    "phone": "string(可选, 11位手机号)",
    "avatar": "string(可选, 头像URL)"
  }
  ```
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "注册成功",
    "data": {
      "token": "JWT_TOKEN",
      "user": {
        "id": 1,
        "email": "user@example.com",
        "nickName": "用户昵称",
        "phone": "13800138000",
        "avatar": "http://example.com/avatar.jpg",
        "createdAt": "2023-01-01T00:00:00",
        "updatedAt": "2023-01-01T00:00:00"
      }
    }
  }
  ```

#### 用户登录
- **接口名称**: 用户登录
- **请求方法**: POST
- **URL路径**: `/auth/login`
- **是否需要认证**: 否
- **前端依赖模块**: 认证模块 (Auth Module)
- **请求参数**:
  ```json
  {
    "email": "string(必填, 邮箱格式)",
    "password": "string(必填)"
  }
  ```
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "登录成功",
    "data": {
      "token": "JWT_TOKEN",
      "user": {
        "id": 1,
        "email": "user@example.com",
        "nickName": "用户昵称",
        "phone": "13800138000",
        "avatar": "http://example.com/avatar.jpg"
      }
    }
  }
  ```

### 3.2 用户管理模块（User Management）

#### 获取用户信息
- **接口名称**: 获取用户信息
- **请求方法**: GET
- **URL路径**: `/api/user/profile`
- **是否需要认证**: 是
- **前端依赖模块**: 用户信息模块 (User Profile Module)
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "成功",
    "data": {
      "id": 1,
      "email": "user@example.com",
      "nickName": "用户昵称",
      "phone": "13800138000",
      "avatar": "http://example.com/avatar.jpg",
      "createdAt": "2023-01-01T00:00:00",
      "updatedAt": "2023-01-01T00:00:00"
    }
  }
  ```

#### 更新用户信息
- **接口名称**: 更新用户信息
- **请求方法**: PUT
- **URL路径**: `/api/user/profile`
- **是否需要认证**: 是
- **前端依赖模块**: 用户信息模块 (User Profile Module)
- **请求参数**:
  ```json
  {
    "nickName": "string(可选)",
    "phone": "string(可选)",
    "avatar": "string(可选)"
  }
  ```

#### 根据ID获取用户
- **接口名称**: 根据ID获取用户
- **请求方法**: GET
- **URL路径**: `/api/user/{id}`
- **是否需要认证**: 是
- **前端依赖模块**: 用户信息模块 (User Profile Module)
- **路径参数**: id(用户ID)
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "成功",
    "data": {
      "id": 1,
      "email": "user@example.com",
      "nickName": "用户昵称",
      "avatar": "http://example.com/avatar.jpg"
    }
  }
  ```

### 3.3 目标管理模块（Goals Management）

#### 创建目标
- **接口名称**: 创建目标
- **请求方法**: POST
- **URL路径**: `/api/goals`
- **是否需要认证**: 是
- **前端依赖模块**: 目标管理模块 (Goal Management Module)
- **请求参数**:
  ```json
  {
    "title": "string(必填, 1-100字符)",
    "description": "string(可选, 最大500字符)",
    "categoryId": "number(必填, 分类ID)",
    "priority": "string(必填, high/medium/low)",
    "status": "string(必填, in_progress/completed/pending)",
    "startDate": "string(可选, YYYY-MM-DD)",
    "endDate": "string(可选, YYYY-MM-DD)",
    "icon": "string(可选, 图标URL或标识)"
  }
  ```
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "创建成功",
    "data": {
      "id": 1,
      "title": "目标标题",
      "description": "目标描述",
      "categoryId": 1,
      "userId": 1,
      "priority": "high",
      "status": "in_progress",
      "startDate": "2023-01-01",
      "endDate": "2023-12-31",
      "icon": "target-icon",
      "createdAt": "2023-01-01T00:00:00",
      "updatedAt": "2023-01-01T00:00:00"
    }
  }
  ```

#### 获取目标列表
- **接口名称**: 获取目标列表
- **请求方法**: GET
- **URL路径**: `/api/goals`
- **是否需要认证**: 是
- **前端依赖模块**: 目标管理模块 (Goal Management Module)
- **查询参数**:
  - page: 页码(从0开始, 默认0)
  - size: 每页数量(默认10, 最大100)
  - categoryId: 分类ID(可选)
  - status: 状态筛选(可选)
  - sort: 排序字段(默认createdAt)
  - order: 排序方向(asc/desc, 默认desc)
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "成功",
    "data": {
      "content": [
        {
          "id": 1,
          "title": "目标标题",
          "description": "目标描述",
          "categoryId": 1,
          "priority": "high",
          "status": "in_progress",
          "startDate": "2023-01-01",
          "endDate": "2023-12-31",
          "icon": "target-icon",
          "createdAt": "2023-01-01T00:00:00",
          "updatedAt": "2023-01-01T00:00:00"
        }
      ],
      "totalElements": 100,
      "totalPages": 10,
      "size": 10,
      "number": 0,
      "first": true,
      "last": false
    }
  }
  ```

#### 目标详情
- **接口名称**: 目标详情
- **请求方法**: GET
- **URL路径**: `/api/goals/{id}`
- **是否需要认证**: 是
- **前端依赖模块**: 目标管理模块 (Goal Management Module)
- **路径参数**: id(目标ID)
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "成功",
    "data": {
      "id": 1,
      "title": "目标标题",
      "description": "目标描述",
      "categoryId": 1,
      "userId": 1,
      "priority": "high",
      "status": "in_progress",
      "startDate": "2023-01-01",
      "endDate": "2023-12-31",
      "icon": "target-icon",
      "createdAt": "2023-01-01T00:00:00",
      "updatedAt": "2023-01-01T00:00:00",
      "category": {
        "id": 1,
        "name": "分类名称",
        "icon": "category-icon"
      }
    }
  }
  ```

#### 更新目标
- **接口名称**: 更新目标
- **请求方法**: PUT
- **URL路径**: `/api/goals/{id}`
- **是否需要认证**: 是
- **前端依赖模块**: 目标管理模块 (Goal Management Module)
- **路径参数**: id(目标ID)
- **请求参数**:
  ```json
  {
    "title": "string(可选)",
    "description": "string(可选)",
    "categoryId": "number(可选)",
    "priority": "string(可选)",
    "status": "string(可选)",
    "startDate": "string(可选)",
    "endDate": "string(可选)",
    "icon": "string(可选)"
  }
  ```

#### 删除目标
- **接口名称**: 删除目标
- **请求方法**: DELETE
- **URL路径**: `/api/goals/{id}`
- **是否需要认证**: 是
- **前端依赖模块**: 目标管理模块 (Goal Management Module)
- **路径参数**: id(目标ID)
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "删除成功",
    "data": null
  }
  ```

#### 批量删除目标
- **接口名称**: 批量删除目标
- **请求方法**: DELETE
- **URL路径**: `/api/goals/batch`
- **是否需要认证**: 是
- **前端依赖模块**: 目标管理模块 (Goal Management Module)
- **请求参数**:
  ```json
  [1, 2, 3] // 目标ID数组
  ```
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "批量删除成功",
    "data": {
      "deletedCount": 3
    }
  }
  ```

### 3.4 子目标管理模块（Sub-goals Management）

#### 创建子目标
- **接口名称**: 创建子目标
- **请求方法**: POST
- **URL路径**: `/api/sub-goal`
- **是否需要认证**: 是
- **前端依赖模块**: 子目标管理模块 (Sub-goal Management Module)
- **请求参数**:
  ```json
  {
    "goalId": "number(必填, 所属目标ID)",
    "title": "string(必填, 1-100字符)",
    "description": "string(可选, 最大500字符)",
    "priority": "string(必填, high/medium/low)",
    "status": "string(必填, in_progress/completed/pending)",
    "dueDate": "string(可选, YYYY-MM-DD)",
    "progress": "number(可选, 0-100)"
  }
  ```
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "创建成功",
    "data": {
      "id": 1,
      "goalId": 1,
      "title": "子目标标题",
      "description": "子目标描述",
      "priority": "high",
      "status": "in_progress",
      "dueDate": "2023-01-31",
      "progress": 0,
      "createdAt": "2023-01-01T00:00:00",
      "updatedAt": "2023-01-01T00:00:00"
    }
  }
  ```

#### 获取子目标列表
- **接口名称**: 获取子目标列表
- **请求方法**: GET
- **URL路径**: `/api/sub-goal/goal/{goalId}`
- **是否需要认证**: 是
- **前端依赖模块**: 子目标管理模块 (Sub-goal Management Module)
- **路径参数**: goalId(目标ID)
- **查询参数**:
  - status: 状态筛选(可选)
  - sort: 排序字段(默认createdAt)
  - order: 排序方向(asc/desc, 默认desc)
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "成功",
    "data": [
      {
        "id": 1,
        "goalId": 1,
        "title": "子目标标题",
        "description": "子目标描述",
        "priority": "high",
        "status": "in_progress",
        "dueDate": "2023-01-31",
        "progress": 0,
        "createdAt": "2023-01-01T00:00:00",
        "updatedAt": "2023-01-01T00:00:00"
      }
    ]
  }
  ```

#### 子目标详情
- **接口名称**: 子目标详情
- **请求方法**: GET
- **URL路径**: `/api/sub-goal/{id}`
- **是否需要认证**: 是
- **前端依赖模块**: 子目标管理模块 (Sub-goal Management Module)
- **路径参数**: id(子目标ID)
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "成功",
    "data": {
      "id": 1,
      "goalId": 1,
      "title": "子目标标题",
      "description": "子目标描述",
      "priority": "high",
      "status": "in_progress",
      "dueDate": "2023-01-31",
      "progress": 0,
      "createdAt": "2023-01-01T00:00:00",
      "updatedAt": "2023-01-01T00:00:00"
    }
  }
  ```

#### 更新子目标
- **接口名称**: 更新子目标
- **请求方法**: PUT
- **URL路径**: `/api/sub-goal/{id}`
- **是否需要认证**: 是
- **前端依赖模块**: 子目标管理模块 (Sub-goal Management Module)
- **路径参数**: id(子目标ID)
- **请求参数**:
  ```json
  {
    "title": "string(可选)",
    "description": "string(可选)",
    "priority": "string(可选)",
    "status": "string(可选)",
    "dueDate": "string(可选)",
    "progress": "number(可选, 0-100)"
  }
  ```

#### 删除子目标
- **接口名称**: 删除子目标
- **请求方法**: DELETE
- **URL路径**: `/api/sub-goal/{id}`
- **是否需要认证**: 是
- **前端依赖模块**: 子目标管理模块 (Sub-goal Management Module)
- **路径参数**: id(子目标ID)
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "删除成功",
    "data": null
  }
  ```

#### 批量删除子目标
- **接口名称**: 批量删除子目标
- **请求方法**: DELETE
- **URL路径**: `/api/sub-goal/batch`
- **是否需要认证**: 是
- **前端依赖模块**: 子目标管理模块 (Sub-goal Management Module)
- **请求参数**:
  ```json
  [1, 2, 3] // 子目标ID数组
  ```
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "批量删除成功",
    "data": {
      "deletedCount": 3
    }
  }
  ```

### 3.5 分类管理模块（Categories Management）

#### 获取分类列表
- **接口名称**: 获取分类列表
- **请求方法**: GET
- **URL路径**: `/api/categories`
- **是否需要认证**: 是
- **前端依赖模块**: 分类管理模块 (Category Management Module)
- **查询参数**:
  - page: 页码(从0开始, 默认0)
  - size: 每页数量(默认10, 最大100)
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "成功",
    "data": {
      "content": [
        {
          "id": 1,
          "name": "分类名称",
          "icon": "category-icon",
          "userId": 1,
          "createdAt": "2023-01-01T00:00:00",
          "updatedAt": "2023-01-01T00:00:00",
          "goalCount": 5 // 该分类下的目标数量
        }
      ],
      "totalElements": 10,
      "totalPages": 1,
      "size": 10,
      "number": 0,
      "first": true,
      "last": true
    }
  }
  ```

#### 创建分类
- **接口名称**: 创建分类
- **请求方法**: POST
- **URL路径**: `/api/categories`
- **是否需要认证**: 是
- **前端依赖模块**: 分类管理模块 (Category Management Module)
- **请求参数**:
  ```json
  {
    "name": "string(必填, 1-20字符)",
    "icon": "string(可选, 图标URL或标识)"
  }
  ```
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "创建成功",
    "data": {
      "id": 1,
      "name": "分类名称",
      "icon": "category-icon",
      "userId": 1,
      "createdAt": "2023-01-01T00:00:00",
      "updatedAt": "2023-01-01T00:00:00"
    }
  }
  ```

#### 更新分类
- **接口名称**: 更新分类
- **请求方法**: PUT
- **URL路径**: `/api/categories/{id}`
- **是否需要认证**: 是
- **前端依赖模块**: 分类管理模块 (Category Management Module)
- **路径参数**: id(分类ID)
- **请求参数**:
  ```json
  {
    "name": "string(可选)",
    "icon": "string(可选)"
  }
  ```

#### 删除分类
- **接口名称**: 删除分类
- **请求方法**: DELETE
- **URL路径**: `/api/categories/{id}`
- **是否需要认证**: 是
- **前端依赖模块**: 分类管理模块 (Category Management Module)
- **路径参数**: id(分类ID)
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "删除成功",
    "data": null
  }
  ```

### 3.6 统计模块（Statistics）

#### 目标统计
- **接口名称**: 目标统计
- **请求方法**: GET
- **URL路径**: `/api/statistics/goals`
- **是否需要认证**: 是
- **前端依赖模块**: 统计分析模块 (Statistics Module)
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "成功",
    "data": {
      "total": 20, // 总目标数
      "completed": 10, // 已完成数
      "inProgress": 7, // 进行中数
      "pending": 3 // 待开始数
    }
  }
  ```

#### 仪表盘统计
- **接口名称**: 仪表盘统计
- **请求方法**: GET
- **URL路径**: `/api/statistics/dashboard`
- **是否需要认证**: 是
- **前端依赖模块**: 统计分析模块 (Statistics Module)
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "成功",
    "data": {
      "user": {
        "id": 1,
        "nickName": "用户昵称",
        "avatar": "http://example.com/avatar.jpg"
      },
      "goals": {
        "total": 20,
        "completed": 10,
        "inProgress": 7,
        "pending": 3,
        "recentCompleted": [
          // 最近完成的目标
        ]
      },
      "categories": {
        "total": 5,
        "mostUsed": {
          "id": 1,
          "name": "最常用分类",
          "goalCount": 8
        }
      }
    }
  }
  ```

#### 趋势统计
- **接口名称**: 趋势统计
- **请求方法**: GET
- **URL路径**: `/api/statistics/trend`
- **是否需要认证**: 是
- **前端依赖模块**: 统计分析模块 (Statistics Module)
- **查询参数**:
  - period: 统计周期(week/month/year, 默认month)
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "成功",
    "data": {
      "period": "month",
      "data": [
        {
          "date": "2023-01-01",
          "created": 2, // 当日创建的目标数
          "completed": 1 // 当日完成的目标数
        }
        // ... 更多日期数据
      ]
    }
  }
  ```

#### 分类统计
- **接口名称**: 分类统计
- **请求方法**: GET
- **URL路径**: `/api/statistics/categories`
- **是否需要认证**: 是
- **前端依赖模块**: 统计分析模块 (Statistics Module)
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "成功",
    "data": [
      {
        "categoryId": 1,
        "categoryName": "分类名称",
        "goalCount": 8, // 该分类下的目标总数
        "completedCount": 5, // 该分类下已完成的目标数
        "completionRate": 62.5 // 完成率百分比
      }
      // ... 更多分类数据
    ]
  }
  ```

### 3.7 成就系统模块（Achievements）

#### 获取成就列表
- **接口名称**: 获取成就列表
- **请求方法**: GET
- **URL路径**: `/api/achievements`
- **是否需要认证**: 是
- **前端依赖模块**: 成就系统模块 (Achievements Module)
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "成功",
    "data": [
      {
        "id": 1,
        "name": "成就名称",
        "description": "成就描述",
        "icon": "achievement-icon",
        "unlockedAt": "2023-01-01T00:00:00", // 解锁时间
        "condition": {
          "type": "goal_count",
          "value": 10,
          "description": "创建10个目标"
        }
      }
      // ... 更多成就
    ]
  }
  ```

#### 检查并解锁成就
- **接口名称**: 检查并解锁成就
- **请求方法**: POST
- **URL路径**: `/api/achievements/check`
- **是否需要认证**: 是
- **前端依赖模块**: 成就系统模块 (Achievements Module)
- **请求体**: 无(使用用户当前状态自动检查)
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "检查完成",
    "data": {
      "newUnlocked": [
        {
          "id": 1,
          "name": "成就名称",
          "description": "成就描述",
          "icon": "achievement-icon",
          "unlockedAt": "2023-01-01T00:00:00"
        }
      ],
      "totalUnlocked": 5
    }
  }
  ```

## 四、关键参数验证规则

### 4.1 通用规则
- 字符串参数去除首尾空格
- 日期格式: YYYY-MM-DD
- 时间格式: ISO 8601 (YYYY-MM-DDTHH:mm:ss)
- ID参数必须为正整数
- 分页参数: page>=0, 1<=size<=100

### 4.2 邮箱验证规则
- 必须符合RFC 5322标准
- 示例: user@example.com

### 4.3 密码验证规则
- 长度: 8-20位
- 必须包含字母和数字
- 可包含特殊字符

### 4.4 优先级枚举值
- high: 高优先级
- medium: 中等优先级
- low: 低优先级

### 4.5 状态枚举值
- pending: 待开始
- in_progress: 进行中
- completed: 已完成
- cancelled: 已取消(仅目标)

## 五、分页响应结构说明

所有分页接口返回的数据结构遵循Spring Data规范:

```json
{
  "content": [], // 当前页数据列表
  "totalElements": 100, // 总记录数
  "totalPages": 10, // 总页数
  "size": 10, // 每页大小
  "number": 0, // 当前页码(从0开始)
  "first": true, // 是否为第一页
  "last": false // 是否为最后一页
}
```

## 六、前端对接注意事项

### 6.1 Token管理
- 登录成功后存储token到本地存储
- 每次API请求携带Authorization头
- token过期时引导用户重新登录

### 6.2 错误处理
- 统一处理各种错误码
- 400错误时展示具体的参数错误信息
- 401错误时清除本地token并跳转登录页
- 404错误时提示资源不存在
- 500错误时显示通用错误提示

### 6.3 数据格式转换
- 日期字符串转换为前端需要的格式
- 枚举值转换为前端显示文本
- 分页数据转换为前端列表组件需要的格式

### 6.4 表单验证
- 前端表单验证规则应与后端保持一致
- 提交前进行必填项检查
- 特殊字段(邮箱、日期等)进行格式验证

### 6.5 数据缓存
- 分类列表、用户信息等不常变的数据可缓存
- 目标列表等动态数据不建议缓存
- 缓存过期策略建议设置为5-30分钟

## 七、未实现接口详细说明

以下接口在后端服务器中尚未实现，需要后续开发。Mock服务器中已提供完整的模拟实现。

### 7.1 用户管理模块

#### 更新用户信息完整实现
- **接口**: `PUT /api/user/profile`
- **是否需要认证**: 是
- **请求参数**:
  ```json
  {
    "nickName": "string(可选, 2-20字符)",
    "phone": "string(可选, 11位手机号)",
    "avatar": "string(可选, 头像URL)"
  }
  ```
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "更新成功",
    "data": {
      "id": 1,
      "email": "user@example.com",
      "nickName": "更新后的昵称",
      "phone": "13800138000",
      "avatar": "http://example.com/new-avatar.jpg",
      "createdAt": "2023-01-01T00:00:00",
      "updatedAt": "2023-01-15T10:30:00"
    }
  }
  ```
- **错误响应**:
  ```json
  {
    "code": 400,
    "message": "昵称长度不能超过20个字符",
    "data": null
  }
  ```

### 7.2 分类管理模块

#### 创建分类完整实现
- **接口名称**: 创建分类
- **请求方法**: POST
- **URL路径**: `/api/categories`
- **是否需要认证**: 是
- **请求参数**:
  ```json
  {
    "name": "string(必填, 1-20字符)",
    "icon": "string(可选, 图标标识)"
  }
  ```
- **业务规则**:
  - 分类名称不能为空且不能超过20个字符
  - 分类名称在用户范围内必须唯一
  - 创建时自动关联当前用户ID
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "创建成功",
    "data": {
      "id": 4,
      "name": "新分类",
      "icon": "custom-icon",
      "userId": 1,
      "createdAt": "2023-01-15T10:30:00",
      "updatedAt": "2023-01-15T10:30:00"
    }
  }
  ```
- **错误码说明**:
  - 400: 请求参数错误
  - 409: 分类名称已存在
  - 500: 服务器内部错误

#### 更新分类完整实现
- **接口名称**: 更新分类
- **请求方法**: PUT
- **URL路径**: `/api/categories/{id}`
- **是否需要认证**: 是
- **路径参数**:
  - id: number(必填, 分类ID)
- **请求参数**:
  ```json
  {
    "name": "string(可选, 1-20字符)",
    "icon": "string(可选, 图标标识)"
  }
  ```
- **业务规则**:
  - 只能更新属于当前用户的分类
  - 分类名称在用户范围内必须唯一
  - 更新后自动更新updatedAt时间
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "更新成功",
    "data": {
      "id": 3,
      "name": "运动",
      "icon": "🤸",
      "userId": 1,
      "createdAt": "2023-01-10T10:00:00",
      "updatedAt": "2023-01-15T10:30:00"
    }
  }
  ```
- **错误码说明**:
  - 400: 请求参数错误
  - 403: 没有权限更新其他用户的分类
  - 404: 分类不存在
  - 409: 分类名称已存在
  - 500: 服务器内部错误

#### 删除分类完整实现
- **接口名称**: 删除分类
- **请求方法**: DELETE
- **URL路径**: `/api/categories/{id}`
- **是否需要认证**: 是
- **路径参数**:
  - id: number(必填, 分类ID)
- **业务规则**:
  - 只能删除属于当前用户的分类
  - 如果分类下还有目标，不允许删除
  - 删除分类不会影响其他用户的相同名称分类
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "成功",
    "data": true
  }
  ```
- **错误响应**:
  ```json
  {
    "code": 400,
    "message": "该分类下还有目标，无法删除",
    "data": null
  }
  ```
- **错误码说明**:
  - 400: 请求参数错误或分类下还有目标
  - 403: 没有权限删除其他用户的分类
  - 404: 分类不存在
  - 500: 服务器内部错误

### 7.3 目标管理模块

#### 目标列表筛选与排序完整实现
- **接口名称**: 获取所有目标
- **请求方法**: GET
- **URL路径**: `/api/goals`
- **是否需要认证**: 是
- **查询参数**:
  - page: number(可选, 页码从0开始, 默认0)
  - size: number(可选, 每页数量, 默认10, 最大100)
  - categoryId: number(可选, 分类ID)
  - status: string(可选, 状态筛选, 值为pending/in_progress/completed/cancelled)
  - sort: string(可选, 排序字段, 默认createdAt, 可选title/priority/status/startDate/endDate)
  - order: string(可选, 排序方向, 值为asc/desc, 默认desc)
- **业务规则**:
  - 只返回属于当前用户的目标
  - 返回数据中包含关联的分类信息
  - 分页数据遵循Spring Data规范
- **错误码说明**:
  - 500: 服务器内部错误

#### 创建目标完整实现
- **接口**: `POST /api/goals`
- **是否需要认证**: 是
- **请求参数**:
  ```json
  {
    "title": "string(必填, 1-100字符)",
    "description": "string(可选, 最大500字符)",
    "categoryId": "number(必填, 分类ID)",
    "priority": "string(必填, high/medium/low)",
    "status": "string(必填, pending/in_progress/completed/cancelled)",
    "startDate": "string(可选, YYYY-MM-DD)",
    "endDate": "string(可选, YYYY-MM-DD)",
    "icon": "string(可选, 图标URL或标识)"
  }
  ```
- **业务规则**:
  - 必须验证分类存在且属于当前用户
  - 优先级和状态必须在枚举值范围内
  - 日期格式必须为YYYY-MM-DD
  - 创建时自动关联当前用户ID

#### 更新目标完整实现
- **接口**: `PUT /api/goals/:id`
- **是否需要认证**: 是
- **路径参数**: id(目标ID)
- **请求参数**: 同创建接口，但所有字段都是可选的
- **业务规则**:
  - 只能更新属于当前用户的目标
  - 如果更新分类ID，必须验证新分类存在且属于当前用户
  - 所有字段的验证规则与创建接口一致

#### 删除目标完整实现
- **接口**: `DELETE /api/goals/:id`
- **是否需要认证**: 是
- **路径参数**: id(目标ID)
- **业务规则**:
  - 只能删除属于当前用户的目标
  - 删除目标时自动删除所有相关的子目标
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "删除成功",
    "data": {
      "deletedSubGoalCount": 3
    }
  }
  ```

#### 批量删除目标完整实现
- **接口**: `DELETE /api/goals/batch`
- **是否需要认证**: 是
- **请求参数**:
  ```json
  [1, 2, 3] // 目标ID数组
  ```
- **业务规则**:
  - 只能删除属于当前用户的目标
  - 删除目标时自动删除所有相关的子目标
  - 返回成功删除的目标数量

### 7.4 子目标管理模块

#### 获取子目标列表完整实现
- **接口**: `GET /api/sub-goal/goal/:goalId`
- **是否需要认证**: 是
- **路径参数**: goalId(目标ID)
- **查询参数**:
  - status: 状态筛选(可选)
  - sort: 排序字段(默认createdAt)
  - order: 排序方向(asc/desc, 默认desc)
- **业务规则**:
  - 只能获取属于当前用户的目标的子目标
  - 验证目标存在且属于当前用户
  - 支持按状态筛选和多种排序方式

#### 创建子目标完整实现
- **接口**: `POST /api/sub-goal`
- **是否需要认证**: 是
- **请求参数**:
  ```json
  {
    "goalId": "number(必填, 所属目标ID)",
    "title": "string(必填, 1-100字符)",
    "description": "string(可选, 最大500字符)",
    "priority": "string(必填, high/medium/low)",
    "status": "string(必填, pending/in_progress/completed/cancelled)",
    "dueDate": "string(可选, YYYY-MM-DD)",
    "progress": "number(可选, 0-100)"
  }
  ```
- **业务规则**:
  - 必须验证目标存在且属于当前用户
  - 进度值必须在0-100之间
  - 截止日期格式必须为YYYY-MM-DD

#### 更新子目标完整实现
- **接口**: `PUT /api/sub-goal/:id`
- **是否需要认证**: 是
- **路径参数**: id(子目标ID)
- **请求参数**: 同创建接口，但所有字段都是可选的
- **业务规则**:
  - 只能更新属于当前用户的目标的子目标
  - 所有字段的验证规则与创建接口一致

#### 删除子目标完整实现
- **接口**: `DELETE /api/sub-goal/:id`
- **是否需要认证**: 是
- **路径参数**: id(子目标ID)
- **业务规则**:
  - 只能删除属于当前用户的目标的子目标
  - 删除操作不会影响所属目标或其他子目标

#### 批量删除子目标完整实现
- **接口**: `DELETE /api/sub-goal/batch`
- **是否需要认证**: 是
- **请求参数**:
  ```json
  [1, 2, 3] // 子目标ID数组
  ```
- **业务规则**:
  - 只能删除属于当前用户的目标的子目标
  - 返回成功删除的子目标数量

### 7.5 统计模块

#### 获取目标统计信息完整实现
- **接口名称**: 获取目标统计信息
- **请求方法**: GET
- **URL路径**: `/api/statistics/goals`
- **是否需要认证**: 是
- **业务规则**:
  - 返回用户目标的统计数据，包括总数、各状态数量和逾期数量
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "成功",
    "data": {
      "total": 10,
      "completed": 3,
      "inProgress": 5,
      "pending": 2,
      "cancelled": 0,
      "overdue": 1
    }
  }
  ```
- **错误码说明**:
  - 500: 服务器内部错误

#### 仪表盘统计完整实现
- **接口名称**: 获取仪表盘统计信息
- **请求方法**: GET
- **URL路径**: `/api/statistics/dashboard`
- **是否需要认证**: 是
- **业务规则**:
  - 返回用户基本信息
  - 返回目标统计（总数、已完成、进行中、待开始、已取消、逾期）
  - 返回用户ID和用户名
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "成功",
    "data": {
      "goalStatistics": {
        "total": 10,
        "completed": 3,
        "inProgress": 5,
        "pending": 2,
        "cancelled": 0,
        "overdue": 1
      },
      "username": "用户昵称",
      "userId": 1
    }
  }
  ```
- **错误码说明**:
  - 500: 服务器内部错误

#### 趋势统计完整实现
- **接口**: `GET /api/statistics/trend`
- **是否需要认证**: 是
- **查询参数**:
  - period: 统计周期(week/month/year, 默认month)
- **业务规则**:
  - 根据周期返回相应天数的数据
  - 每条数据包含日期、创建数量和完成数量
  - 数据按日期升序排列
- **特殊说明**:
  - week: 返回最近7天的数据
  - month: 返回最近30天的数据
  - year: 返回最近365天的数据

#### 分类统计完整实现
- **接口**: `GET /api/statistics/categories`
- **是否需要认证**: 是
- **业务规则**:
  - 返回当前用户的所有分类统计
  - 包含每个分类的目标总数、完成数和完成率
  - 分类按目标数量降序排列

### 7.6 成就系统模块

#### 获取成就列表完整实现
- **接口名称**: 获取用户的所有成就
- **请求方法**: GET
- **URL路径**: `/api/achievements`
- **是否需要认证**: 是
- **业务规则**:
  - 返回所有成就，包含解锁状态和解锁时间
  - 已解锁的成就显示unlockTime时间
  - 未解锁的成就不显示unlockTime时间
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "成功",
    "data": [
      {
        "id": 1,
        "name": "第一步",
        "description": "创建第一个目标",
        "iconUrl": "/images/achievements/first_goal.png",
        "userId": 1,
        "unlockTime": "2025-12-10T10:00:00",
        "createdAt": "2025-12-10T10:00:00",
        "updatedAt": "2025-12-10T10:00:00"
      },
      {
        "id": 2,
        "name": "目标达人",
        "description": "创建5个目标",
        "iconUrl": "/images/achievements/five_goals.png",
        "userId": 1,
        "unlockTime": "2025-12-15T10:00:00",
        "createdAt": "2025-12-15T10:00:00",
        "updatedAt": "2025-12-15T10:00:00"
      }
    ]
  }
  ```
- **错误码说明**:
  - 500: 服务器内部错误

#### 检查并解锁成就完整实现
- **接口名称**: 检查并解锁成就
- **请求方法**: POST
- **URL路径**: `/api/achievements/check`
- **是否需要认证**: 是
- **业务规则**:
  - 根据用户当前数据自动检查成就条件
  - 如果满足条件且未解锁，则自动解锁
  - 支持多种成就类型：目标数量、连续完成天数等
- **成功响应**:
  ```json
  {
    "code": 200,
    "message": "成功",
    "data": null
  }
  ```
- **错误码说明**:
  - 500: 服务器内部错误

## 八、Mock服务器实现说明

### 8.1 完整版Mock服务器
- **文件位置**: `mock-server/server-complete.js`
- **启动命令**: `node mock-server/server-complete.js`
- **端口**: 8081
- **特点**: 实现了所有API接口的完整模拟，包括业务逻辑、数据验证和错误处理

### 8.2 Mock数据说明
- **测试账户**: test@example.com / password123
- **用户数据**: 支持注册、登录、更新个人信息
- **分类数据**: 预置健康、学习、工作三个分类
- **目标数据**: 预置示例目标，包含不同状态和优先级
- **子目标数据**: 预置示例子目标，展示不同进度状态
- **成就数据**: 预置三种成就类型，支持自动解锁

### 8.3 使用建议
1. 前端开发阶段使用完整版Mock服务器(`server-complete.js`)
2. 与后端联调时切换到真实后端服务器
3. 通过环境配置文件(`config/env.js`)轻松切换环境
4. 使用验证脚本(`verify-api-config.js`)检查API路径配置

这份API规范解析文档将作为前端开发和接口联调的参考依据，确保前后端数据交互的一致性和准确性。