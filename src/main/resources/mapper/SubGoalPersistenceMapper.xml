<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.crazydream.infrastructure.persistence.mapper.SubGoalPersistenceMapper">
    
    <insert id="insert" parameterType="com.crazydream.infrastructure.persistence.po.SubGoalPO" 
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sub_goal (goal_id, title, description, progress, status, create_time, update_time)
        VALUES (#{goalId}, #{title}, #{description}, #{progress}, #{status}, #{createTime}, #{updateTime})
    </insert>
    
    <update id="update" parameterType="com.crazydream.infrastructure.persistence.po.SubGoalPO">
        UPDATE sub_goal
        SET title = #{title},
            description = #{description},
            progress = #{progress},
            status = #{status},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>
    
    <select id="selectById" resultType="com.crazydream.infrastructure.persistence.po.SubGoalPO">
        SELECT id, goal_id, title, description, progress, status, create_time, update_time
        FROM sub_goal
        WHERE id = #{id}
    </select>
    
    <select id="selectByGoalId" resultType="com.crazydream.infrastructure.persistence.po.SubGoalPO">
        SELECT id, goal_id, title, description, progress, status, create_time, update_time
        FROM sub_goal
        WHERE goal_id = #{goalId}
        ORDER BY create_time DESC
    </select>
    
    <delete id="deleteById">
        DELETE FROM sub_goal WHERE id = #{id}
    </delete>
    
    <delete id="batchDelete">
        DELETE FROM sub_goal WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    
</mapper>
